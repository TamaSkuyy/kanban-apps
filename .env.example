# Database
POSTGRES_USER=kanban
POSTGRES_PASSWORD=kanban
POSTGRES_DB=kanban
POSTGRES_PORT=5432
DATABASE_URL=postgres://kanban:kanban@localhost:5432/kanban?sslmode=disable

# Backend
BACKEND_ADDR=:8080
JWT_SECRET=change-me
APP_URL=http://localhost:3000
FRONTEND_ORIGIN=http://localhost:3000
RUN_MIGRATIONS=0
SKIP_VERIFY=1
LOG_MAIL_BODY=1

# Email (Resend / SES) — kosong = logMailer (dev)
RESEND_API_KEY=
# Gmail SMTP alternative
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Billing (Stripe) — kosong = stub dev (langsung upgrade tanpa kartu)
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=price_pro_xxx
STRIPE_PRICE_SCALE=price_scale_xxx

# OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8080
